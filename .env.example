# Environment variables for local-chat-client
# Copy this file to .env and adjust values as needed

# --- LangSmith (optional) ---
# API key to enable tracing/monitoring in LangSmith. Leave empty to disable.
LANGSMITH_API_KEY=

# Project name used for grouping traces in LangSmith
LANGSMITH_PROJECT=simple-chat-client

# Override only if using a custom LangSmith endpoint
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com

# --- Ollama (local LLM) ---
# Default local model name (must exist in your Ollama)
OLLAMA_MODEL=gemma3:4b

# Override only if your Ollama server isn't on the default host/port
# OLLAMA_BASE_URL=http://localhost:11434
